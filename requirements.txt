# Exact environment the committed logs, checkpoints, and README numbers were
# produced in (Python 3.12.13, CPU). Install with:
#
#     python -m venv .venv && ./.venv/bin/pip install -r requirements.txt -e .
#
# and reproduce.sh replays every figure from the committed artifacts.
#
# pyproject.toml deliberately keeps LOWER BOUNDS instead (torch>=2.0, ...):
# that is the library's compatibility contract, and CI installs from it (with
# the CPU torch wheel) so the 3.10/3.12 matrix keeps testing against current
# releases. This file is the reproduction lockfile; the two jobs are different,
# so the two files differ. Transitive dependencies (torch's and matplotlib's
# own) are left to pip.
#
# NOTE ON EXACT REPRODUCTION: training is seeded and replays bit-for-bit on the
# same torch build and CPU, but torch does not guarantee cross-version or
# cross-hardware bitwise determinism. The trained models and CSV logs ship as
# committed artifacts precisely so a reader never has to retrain (hours) to
# regenerate the figures.
torch==2.13.0
numpy==2.5.1
matplotlib==3.11.0
pytest==9.1.1
